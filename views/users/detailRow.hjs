<tr>
    <th scope="row" class="table-secondary">{{th}}</th>
    <td>
        <div class="d-flex justify-content-between align-items-center">
            <span>{{editData}}</span>
            <button class="btn btn-sm btn-outline-primary" hx-get="/users/detail/{{userID}}?field={{editDataName}}"
                hx-target="closest td">
                <i class="bi bi-pencil"></i> Edit
            </button>
        </div>
    </td>
</tr>